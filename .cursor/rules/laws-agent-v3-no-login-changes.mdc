---
description: Do not change login or header/footer on Laws Agent v3 unless the user explicitly orders it
globs: agents/laws_agent_v3/**/*
alwaysApply: false
---

# Laws Agent v3 – No Login or Layout Changes

- **Do not modify** any login, session, redirect, or auth logic in `agents/laws_agent_v3/` unless the user explicitly asks you to.
- **Do not remove or replace** `includes/header.php` and `includes/footer.php` on this page. The page must use the standard header/footer includes. Do not make the page "self-contained" or output raw HTML without them.
